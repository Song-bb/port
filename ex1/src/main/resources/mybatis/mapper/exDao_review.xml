<?xml version="1.0" encoding="UTF-8"?>

 <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.example.demo.dao.IDao_review">

	<insert id="addReview"> 
			INSERT INTO review 
			(idx, user_index, item_number, item_img, item_name, order_number, Buy_price, Buy_date, review_content, review_score, write_date) 
			VALUES 
			(0, #{user_id}, #{reason}, #{content}, default)
	</insert>

	
	
</mapper>