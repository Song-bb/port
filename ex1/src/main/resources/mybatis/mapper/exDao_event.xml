<?xml version="1.0" encoding="UTF-8"?>

 <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.example.demo.dao.IDao_event">
	
	<!-- event_list -->
	<select id="event_listDao" resultType="com.example.demo.dto.dto_event"> 
			SELECT * FROM event ORDER BY event_index DESC
	</select>
	
	<!-- event_view -->
	<select id="event_viewDao" resultType="com.example.demo.dto.dto_event"> 
			SELECT * FROM event WHERE event_index = #{event_index}
	</select>
	<!-- event_viewCount -->
	<select id="event_viewCoun" resultType="com.example.demo.dto.dto_event"> 
			UPDATE event SET event_hit = event_hit + 1 WHERE evnet_index = #{event_index}
	</select>
	
	<!-- event_write -->
	<insert id="event_writeDao"> 
			INSERT INTO event (event_index, event_title, event_content, event_banner) VALUES (event_seq.nextval, #{event_title}, #{event_content}, #{event_banner})
	</insert>
	
	<delete id="event_deleteDao"> 
			DELETE FROM event WHERE event_index = #{event_index}
	</delete>
	
	<update id="event_updateDao"> 
			UPDATE event SET event_title = #{event_title}, event_content = #{event_content}, event_banner = #{event_banner} WHERE event_index = #{event_index}
	</update>
	


	
</mapper>
	
