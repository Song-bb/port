<?xml version="1.0" encoding="UTF-8"?>

 <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.example.demo.dao.IDao_members">
	
	<select id="login_ok" resultType="com.example.demo.dto.dto_members"> 
			SELECT * FROM members WHERE user_id = #{user_id} 
	</select>

	<insert id="join_ok"> 
			INSERT INTO members (user_index, user_id, user_pw, user_name, user_email, user_phone, user_address, user_grade, user_point, user_gender, user_birth) VALUES (0, #{user_id}, #{user_pw}, #{user_name}, #{user_email}, #{user_phone}, #{user_address}, "앵두", 0, #{user_gender},#{user_birth})
	</insert>
	
	
</mapper>
	
